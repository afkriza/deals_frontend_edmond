import { VISUAL, COLOR } from 'constants/chart-modal-types';

const chartModalTypes = {
  [VISUAL]: {
    title: 'Visual formatting',
    type: VISUAL
  },
  [COLOR]: {
    title: 'Color formatting',
    type: COLOR
  }
};

export default chartModalTypes;
